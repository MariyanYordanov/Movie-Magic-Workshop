<section id="catalog">
    <h1>All Movies</h1>
    <div class="band">
        {{#each movie}}
            {{> movie}}
        {{/each}}
    </div>
    <div class="no-posts">
        <p class="no-offer">There are no posts yet...</p>
    </div>
</section>