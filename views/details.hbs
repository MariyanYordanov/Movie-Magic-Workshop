{{#with movie}}
<section id="details-page">
    <div class="main_card">
        <div class="card_left">
            <div class="card_datails">
                <h1>{{title}}</h1>
                <div class="card-movie">
                    <div class="first-info">
                        <p class="card-genre">Genre: {{genre}}</p>
                        <p class="card-director">Directror: {{director}}</p>
                    </div>
                    <div class="second-info">
                        <p class="card-year">Year: {{year}}</p>
                        <p class="card-ranking">Rating: {{rating}}</p>
                    </div>

                    <p class="disc">Description: {{description}}</p>
                    {{#if isCreator}}
                    <div class="details-btn">
                        <a id="edit-button" href="/edit/{{_id}}">Edit</a>
                        <a id="delete-button" href="/delete/{{_id}}">Delete</a>
                        <a id="delete-button" href="/attach/{{_id}}">Attach Cast</a>
                    </div>
                    {{/if}}
                </div>
            </div>
        </div>
        <div class="card_right">
            <div class="card_right_img">
                <img src="{{imageURL}}" alt="{{title}}">
            </div>
            <div class="main-cast-card">
                {{#each cast}}
                {{> cast}}
                {{/each}}
            </div>
        </div>
    </div>

</section>
{{/with}}